enum EnumBookingStatus { send, accepted, rejected }

enum EnumChatStatus { send, read }

enum EnumUserStatus { online, offline }
enum EnumUserType { customer, provider }
  enum EnumTypeOfShowChart{
    week,
    month,
    year,
  }

  enum DataView {
  week,
  month,
  year,
}